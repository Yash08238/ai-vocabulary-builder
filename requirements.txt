streamlit>=1.25.0
python-dotenv>=0.21.0
# NOTE: `google-generative-ai` was removed from this file because some platforms
# (including Streamlit Cloud at the time of deploy) cannot find it in the package
# registry. If you need Gemini model support, see `OPTIONAL_REQUIREMENTS.txt`.
# Add any other dependencies if you install them
